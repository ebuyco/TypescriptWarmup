```js
        THe key difference is: Javascript uses "dynamic types" 
        (the resolved at runtime ), Typescript uses "static types" 
        (set during development).
```

```html

        Core types 
      number   1,5.3,10    All number no differentiation between integers and float
      string   'Hi', "Hi", `Hi`   ALl text values
      boolean  true, false        Just these two, no "truthy" or falsy values
      object   {age: 30}    Any Javascript Object, more specific types (type of Object) are possible
      Array    [1,2,3]      Any Javascript array, type can be flexible or strict(regarding the element types)  
      Tuple     [1,2]       Added by the Typescript: Fixed-length array or Fixed type .
      Enum      enum{ NEW, OLD}  Added by Typescript: Automatically enumerated global constant identifiers. 
```